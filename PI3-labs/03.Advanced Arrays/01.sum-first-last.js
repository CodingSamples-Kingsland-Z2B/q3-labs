function main(arr) {
    // let first = +arr.shift();
    // let last = +arr.pop();
    // console.log(first + last);


    console.log(+arr.shift() + +arr.pop());
}

// main(['20', '30', '40'])
main(['5', '10'])