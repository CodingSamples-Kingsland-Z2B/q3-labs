/* 
Write a function, which:
    • Receives 2 numbers: distance and time
    • Calculates the speed needed to travel a given distance for a
        given time
    • Prints the calculated result
 */

function main(distance, time){
    let speed = distance/time;
    console.log(speed);
}

main(15,2);
main(20,5);
main(50,25);