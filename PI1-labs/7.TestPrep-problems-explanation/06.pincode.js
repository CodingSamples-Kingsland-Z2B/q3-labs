// Write a program that generates three-digit PIN codes, the digits of each PIN being within a certain interval. For a PIN
// to be valid, it must satisfy the following conditions:
// ● The first and third digits must be even .
// ● The second digit must be a prime number in the range [2...7] .


function(upperNum1, upperNum2, upperNum3) {

    // 1st loop (for loop (first digit) ) boundries (2- upperNum1) step +2
    // 2nd loop (for loop (second digit) boundries (2-upperNum2) step +1
    if (d2 == 2 || d2 == 3 || d2 == 5 || d2 == 7) {
        // 3rd loop (for loop (third digit)) boundries (2- upperNum3) step +2 
        //  console.log( {d1} {d2}{d3})
    }
}